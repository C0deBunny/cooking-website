# Copy to .env.local and fill in from your Supabase project settings (API section).
# Both values are exposed to the browser by design — the publishable key relies on
# row-level security. Do not put a service-role key in this file.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
